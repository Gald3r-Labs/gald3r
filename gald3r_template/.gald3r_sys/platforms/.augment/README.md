# Augment Code — gald3r Deploy Scaffold

**Config folder**: `.augment/`

This directory is the gald3r deploy scaffold for **Augment Code**. It is registered in
`.gald3r_sys/_platform_capabilities.json` and recognised by the platform-parity
sync tooling.

Authoritative install + customization guide: **`g-skl-platform-augment`** (.gald3r_sys/skills/g-skl-platform-augment/SKILL.md).

> **Status (T1277 — complete):** the full per-platform deploy payload is authored.
> See **`augment_instructions.md`** in this directory for the deploy guide (folder layout, config files,
> conventions, and the T1277 AC6 gitignore decision). The config files this platform
> consumes are shipped alongside it.
