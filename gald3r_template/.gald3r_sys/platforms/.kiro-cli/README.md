# Kiro CLI — gald3r Deploy Scaffold

**Config folder**: shared `.kiro/` (steering / specs / hooks)

This directory is the gald3r deploy scaffold for **Kiro CLI**, Amazon's terminal agent
variant of Kiro. It shares the `.kiro/` configuration with Kiro IDE.

Authoritative install + customization guide: **`g-skl-platform-kiro-cli`** (.gald3r_sys/skills/g-skl-platform-kiro-cli/SKILL.md).

> **Status (T1277 — complete):** see **`kiro-cli_instructions.md`** in this directory for the
> deploy guide. Kiro CLI shares the `.kiro/steering/` files with Kiro IDE (no duplicate
> config); this scaffold carries the CLI-specific guidance and the T1277 AC6 gitignore decision.
