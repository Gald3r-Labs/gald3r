version: 1.0.0
title: g-idea-capture
description: Capture an idea to IDEA_BOARD.md. Activates **g-ideas** → CAPTURE operation.
instructions: 'Capture an idea to IDEA_BOARD.md. Activates **g-ideas** → CAPTURE operation.


  Activate the **g-ideas** skill, CAPTURE operation.


  Capture immediately and return to current work. Never derails the session.'
prompt: Run the gald3r `g-idea-capture` command as described in the instructions.
extensions: []
parameters:
- key: args
  input_type: string
  requirement: optional
  description: Free-form arguments for g-idea-capture, e.g. quoted text or --flag
    values, exactly as you would type them after the command name.
