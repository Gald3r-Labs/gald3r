version: 1.0.0
title: g-review
description: Deprecated — use **@g-code-review** instead.
instructions: 'Deprecated — use **@g-code-review** instead.


  @g-code-review activates the g-skl-code-review skill for full code review functionality.'
prompt: Run the gald3r `g-review` command as described in the instructions.
extensions: []
parameters:
- key: args
  input_type: string
  requirement: optional
  description: Free-form arguments for g-review, e.g. quoted text or --flag values,
    exactly as you would type them after the command name.
