version: 1.0.0
title: g-workspace-status
description: 'Compatibility Alias for `@g-wrkspc-status`: $ARGUMENTS'
instructions: |
  Provisioned by `gald3r platform install`.
  Documentation: https://docs.gald3r.ai
prompt: Run the gald3r `g-workspace-status` command as described in the instructions.
extensions: []
parameters:
- key: args
  input_type: string
  requirement: optional
  description: Free-form arguments for g-workspace-status, e.g. quoted text or --flag
    values, exactly as you would type them after the command name.
