version: 1.0.0
title: g-create-hook
description: Scaffold a new gald3r hook script + companion `hook.md` self-description
  in one step.
instructions: |
  Provisioned by `gald3r platform install`.
  Documentation: https://docs.gald3r.ai
prompt: Run the gald3r `g-create-hook` command as described in the instructions.
extensions: []
parameters:
- key: args
  input_type: string
  requirement: optional
  description: Free-form arguments for g-create-hook, e.g. quoted text or --flag values,
    exactly as you would type them after the command name.
