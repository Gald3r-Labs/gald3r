version: 1.0.0
title: g-wpac-claim
description: 'Register a parent project for this project: $ARGUMENTS'
instructions: |
  Provisioned by `gald3r platform install`.
  Documentation: https://docs.gald3r.ai
prompt: Run the gald3r `g-wpac-claim` command as described in the instructions.
extensions: []
parameters:
- key: args
  input_type: string
  requirement: optional
  description: Free-form arguments for g-wpac-claim, e.g. quoted text or --flag values,
    exactly as you would type them after the command name.
