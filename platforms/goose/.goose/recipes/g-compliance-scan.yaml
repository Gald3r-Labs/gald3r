version: 1.0.0
title: g-compliance-scan
description: 'Activate the g-skl-compliance skill and run a SCAN operation: $ARGUMENTS'
instructions: |
  Provisioned by `gald3r platform install`.
  Documentation: https://docs.gald3r.ai
prompt: Run the gald3r `g-compliance-scan` command as described in the instructions.
extensions: []
parameters:
- key: args
  input_type: string
  requirement: optional
  description: Free-form arguments for g-compliance-scan, e.g. quoted text or --flag
    values, exactly as you would type them after the command name.
