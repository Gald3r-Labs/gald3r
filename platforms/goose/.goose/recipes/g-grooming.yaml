version: 1.0.0
title: g-grooming
description: Deprecated — use **g-medic** instead.
instructions: 'Deprecated — use **g-medic** instead.


  @g-medic handles all cleanup, grooming, and upgrade operations.'
prompt: Run the gald3r `g-grooming` command as described in the instructions.
extensions: []
parameters:
- key: args
  input_type: string
  requirement: optional
  description: Free-form arguments for g-grooming, e.g. quoted text or --flag values,
    exactly as you would type them after the command name.
