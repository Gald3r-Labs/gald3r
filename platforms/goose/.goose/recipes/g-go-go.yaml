version: 1.0.0
title: g-go-go
description: 'Maximal workspace swarm autopilot — rolling implement/review until a
  hard stop: $ARGUMENTS'
instructions: |
  Run: `gald3r autopilot loop`
  Documentation: https://docs.gald3r.ai
prompt: Run the gald3r `g-go-go` command as described in the instructions.
extensions: []
parameters:
- key: args
  input_type: string
  requirement: optional
  description: Free-form arguments for g-go-go, e.g. quoted text or --flag values,
    exactly as you would type them after the command name.
