version: 1.0.0
title: g-wpac-send-to
description: 'Send files, features, specs, ideas, or code to a related project in
  the ecosystem: $ARGUMENTS'
instructions: |
  Provisioned by `gald3r platform install`.
  Documentation: https://docs.gald3r.ai
prompt: Run the gald3r `g-wpac-send-to` command as described in the instructions.
extensions: []
parameters:
- key: args
  input_type: string
  requirement: optional
  description: Free-form arguments for g-wpac-send-to, e.g. quoted text or --flag
    values, exactly as you would type them after the command name.
