version: 1.0.0
title: g-task-sync-check
description: Validate TASKS.md matches tasks/ files. Activates **g-tasks** → SYNC
  CHECK operation.
instructions: 'Validate TASKS.md matches tasks/ files. Activates **g-tasks** → SYNC
  CHECK operation.


  Activate the **g-tasks** skill, SYNC CHECK operation.


  Run at session start or when phantom/orphan issues are suspected.'
prompt: Run the gald3r `g-task-sync-check` command as described in the instructions.
extensions: []
parameters:
- key: args
  input_type: string
  requirement: optional
  description: Free-form arguments for g-task-sync-check, e.g. quoted text or --flag
    values, exactly as you would type them after the command name.
