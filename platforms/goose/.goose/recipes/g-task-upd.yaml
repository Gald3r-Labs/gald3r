version: 1.0.0
title: g-task-upd
description: Update task status or fields. Activates **g-skl-tasks** → UPDATE STATUS
  operation.
instructions: |
  Provisioned by `gald3r platform install`.
  Documentation: https://docs.gald3r.ai
prompt: Run the gald3r `g-task-upd` command as described in the instructions.
extensions: []
parameters:
- key: args
  input_type: string
  requirement: optional
  description: Free-form arguments for g-task-upd, e.g. quoted text or --flag values,
    exactly as you would type them after the command name.
